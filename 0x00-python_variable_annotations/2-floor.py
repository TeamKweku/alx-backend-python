#!/usr/bin/env python3
"""a module that implements a floor function"""

import math


def floor(n: float) -> int:
    """
    Returns the floor of a float.

    Parameters:
        n (float): The input float.

    Returns:
        int: The floor of the input float.
    """
    return math.floor(n)
